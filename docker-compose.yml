version: '3.8'
services:
  code-iq:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - SPRING_PROFILES_ACTIVE=serving
      - CODEIQ_GRAPH_PATH=/app/data/graph.db
